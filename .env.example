# Production (aaPanel): https://smkn11kabtang.sch.id — Laravel + React same-origin
# "/" = same-origin (frontend & backend di domain yang sama, /api via Laravel)
# Untuk dev lokal, ganti ke http://localhost:8000
VITE_API_URL=/

# Contoh prod eksplisit (opsional):
# VITE_API_URL=https://smkn11kabtang.sch.id
